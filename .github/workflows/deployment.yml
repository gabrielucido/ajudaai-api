name: Deployment

on: [push]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Server
        run: |
            cp .env.sample .env

      - name: Run Server
        run: |
            docker-compose up -d
